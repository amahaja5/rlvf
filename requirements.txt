# Core dependencies for RL Voice Leading project
numpy>=1.20.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0
pyyaml>=5.4.0
tqdm>=4.62.0

# MIDI and audio processing
pretty-midi>=0.2.9
midi2audio>=0.1.1
librosa>=0.9.0

# Optional: For better audio synthesis
# FluidSynth system library required for midi2audio
# Install on Ubuntu/Debian: sudo apt-get install fluidsynth
# Install on macOS: brew install fluid-synth
